# Python Service Template

Use this template as a starting point for new Python services built with FastAPI.

## Usage

```bash
cp -r services/py/templates/service services/py/my-service
```

Then customize `Pipfile`, add your logic in `service.py`, and write tests under `tests/`.
