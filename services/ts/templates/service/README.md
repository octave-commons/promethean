# TypeScript Service Template

Use this template as a starting point for new TypeScript services.

## Usage

```bash
cp -r services/ts/templates/service services/ts/my-service
```

Then update `package.json`, add your code under `src/`, and implement tests under `tests/`.
