These tests mirror the original DocOps package tests and verify key utilities
such as markdown parsing, relations, and footer generation. They were moved to
Piper to live alongside the generic pipeline implementation.

