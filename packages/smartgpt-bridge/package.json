{
  "name": "@promethean/smartgpt-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CustomGPT ↔ Promethean bridge: Chroma search, files/grep/stacktrace, TS symbols, and background agent supervisor. Funnel-ready.",
  "scripts": {
    "start": "node dist/fastifyServer.js",
    "reindex": "node dist/cli-reindex.js",
    "symbols": "node dist/cli-symbols.js",
    "test": "pnpm -r --workspace-concurrency=1 --filter @promethean/smartgpt-bridge... run --if-present build && ava",
    "test:watch": "pnpm -r --workspace-concurrency=1 --filter @promethean/smartgpt-bridge... run --if-present build && ava -w",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "bash -c 'pnpm exec eslint src/tests/unit src/tests/integration src/tests/system src/tests/helpers; L1=$?; pnpm run -s lint:tests; L2=$?; exit $((L1|L2))'",
    "lint:tests": "node --input-type=module -e \"import fs from 'fs';import path from 'path';const walk=d=>fs.readdirSync(d).some(f=>{const p=path.join(d,f);const s=fs.statSync(p);return s.isDirectory()?walk(p):p.endsWith('.test.js');});if(walk('src/tests')){console.error('Error: compiled test artifacts found');process.exit(1);} \"",
    "format": "pnpm exec prettier --write src src/tests/unit src/tests/integration src/tests/system src/tests/helpers",
    "coverage": "pnpm run build && c8 -r text -r text-summary -r lcov -r html ava --config ../../config/ava.config.mjs"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.3",
    "@promethean/embedding": "workspace:*",
    "@promethean/file-indexer": "workspace:*",
    "@promethean/fs": "workspace:*",
    "@promethean/indexer-core": "workspace:*",
    "@promethean/indexer-service": "workspace:*",
    "@promethean/level-cache": "workspace:*",
    "@promethean/persistence": "workspace:*",
    "@promethean/utils": "workspace:*",
    "ajv-formats": "^3.0.1",
    "chromadb": "^1.8.1",
    "dotenv": "^17.2.1",
    "duckduckgo-search": "^1.0.7",
    "execa": "^9.6.0",
    "fastify": "^5.3.2",
    "ignore": "^7.0.5",
    "jose": "^5.10.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.8.0",
    "nanoid": "^5.0.7",
    "node-fetch": "^3.3.2",
    "node-pty": "^1.0.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@promethean/test-utils": "workspace:*",
    "@types/sinon": "^17.0.4",
    "@types/uuid": "^9.0.7",
    "esmock": "^2.7.2",
    "mongodb-memory-server": "^10.2.0",
    "sinon": "^17.0.1",
    "tsx": "^4.19.0"
  }
}
