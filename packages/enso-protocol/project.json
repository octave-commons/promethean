{
  "name": "@promethean/enso-protocol",
  "sourceRoot": "packages/enso-protocol/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm --filter @promethean/enso-protocol run build" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm --filter @promethean/enso-protocol run serve" }
    }
  },
  "tags": []
}
