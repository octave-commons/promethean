{
  "name": "webcrawler",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "pnpm run build && ava --config ../../config/ava.config.mjs dist/test/**/*.js",
    "lint": "pnpm exec biome lint .",
    "format": "pnpm exec biome format --write ."
  },
  "dependencies": {
    "@promethean/web": "file:../web",
    "cheerio": "^1.0.0-rc.12"
  },
  "devDependencies": {
    "@types/node": "^20.19.11",
    "ava": "^6.4.1",
    "c8": "^10.1.3",
    "typescript": "^5.9.2"
  }
}
