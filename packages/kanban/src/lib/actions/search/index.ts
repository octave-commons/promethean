/**
 * Barrel exports for search/indexing actions
 */

// Re-export legacy implementations until functional search actions are implemented
export { indexForSearch, searchTasks } from '../../kanban.js';
