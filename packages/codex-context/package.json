{
  "name": "@promethean/codex-context",
  "version": "0.1.0",
  "description": "OpenAI-compatible context service bridging Codex to local LLMs with SmartGPT repo context.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:check": "tsc --noEmit --incremental false",
    "test": "pnpm run build && ava --config ../../config/ava.config.mjs",
    "coverage": "pnpm run build && c8 ava --config ../../config/ava.config.mjs",
    "lint": "pnpm exec biome lint . && pnpm run lint:tests",
    "lint:tests": "node --input-type=module -e \"import fs from 'fs';import path from 'path';const walk=d=>fs.readdirSync(d).some(f=>{const p=path.join(d,f);const s=fs.statSync(p);return s.isDirectory()?walk(p):p.endsWith('.test.js');});if(walk('src/tests')){console.error('Error: compiled test artifacts found');process.exit(1);}\"",
    "format": "pnpm exec biome format --write .",
    "start": "pnpm run build && node --env-file=../../.env dist/index.js",
    "start:dev": "node --env-file=../../.env --loader ts-node/esm src/index.ts"
  },
  "dependencies": {
    "@promethean/utils": "workspace:*",
    "@types/body-parser": "^1.19.5",
    "body-parser": "^1.20.3",
    "date-fns": "^4.1.0",
    "dotenv": "^17.2.1",
    "express": "^4.21.2",
    "mkdirp": "^3.0.1",
    "node-fetch": "^3.3.2",
    "ollama": "^0.5.16",
    "@promethean/pm2-helpers": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/supertest": "^2.0.13",
    "eslint": "^8.57.0",
    "eslint-config-neon": "^0.1.62",
    "prettier": "^3.4.2",
    "supertest": "^7.1.1"
  }
}
