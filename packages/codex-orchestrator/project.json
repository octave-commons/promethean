{
  "name": "ts-codex-orchestrator",
  "root": "packages/codex-orchestrator",
  "sourceRoot": "packages/codex-orchestrator/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -b",
        "cwd": "packages/codex-orchestrator"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "packages/codex-orchestrator"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ava",
        "cwd": "packages/codex-orchestrator"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint .",
        "cwd": "packages/codex-orchestrator"
      }
    }
  },
  "tags": ["scope:packages"]
}
