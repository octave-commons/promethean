# Server Configuration
PORT=3000
HOST=localhost
NODE_ENV=development
LOG_LEVEL=info

# CORS Configuration
CORS_ORIGIN=*

# Authentication (optional - if not set, auth is disabled)
# BEARER_AUTH_KEY=your-secret-bearer-token-here

# Database Configuration (for future implementation)
# DATABASE_URL=postgresql://user:password@localhost:5432/dualstore
# DATABASE_POOL_SIZE=10

# SSE Configuration
SSE_HEARTBEAT_INTERVAL=30000
SSE_POLLING_INTERVAL=5000

# API Configuration
API_PREFIX=/api/v1
API_VERSION=1.0.0

# Rate Limiting (future implementation)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Cache Configuration (future implementation)
# CACHE_TTL_SECONDS=300
# CACHE_MAX_SIZE=1000