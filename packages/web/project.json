{
    "name": "ts-web",
    "root": "shared/ts/web",
    "sourceRoot": "shared/ts/web/src",
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "options": {
                "command": "tsc -b"
            },
            "outputs": ["{projectRoot}/dist"]
        },
        "typecheck": {
            "executor": "nx:run-commands",
            "options": {
                "command": "tsc -p tsconfig.json --noEmit"
            }
        },
        "test": {
            "executor": "nx:run-commands",
            "options": {
                "command": "ava"
            }
        },
        "lint": {
            "executor": "nx:run-commands",
            "options": {
                "command": "eslint ."
            }
        }
    },
    "tags": ["scope:shared-ts"]
}
