{
  "name": "@promethean/<%= name %>",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist",
    "test": "pnpm build && ava",
    "coverage": "pnpm build && c8 ava"
  },
  "dependencies": {}
}
