review all PR associated with the current repo

I can do that, but I need a couple of details:

- Which hosting service is the repo on (GitHub, GitLab, Bitbucket, etc.)?
- Do you want all PRs (open, merged, closed) or only open ones?
- What level of detail do you want per PR? (title/author/state/labels/reviewers/last updated, plus a high-level diff summary or full diff)
- Should I use the GitHub CLI (gh) or the API (requires a token)?

If you’re on GitHub and okay with using gh, I can:
- fetch all PRs for the current repo, and
- produce a concise digest plus a per-PR quick-action note (e.g., merge status, required reviews, suggested next steps).

Shall I proceed with a GitHub-based pull-request review using gh CLI? If yes, I’ll start by detecting the repo remotes and listing PRs.

riatzukiza/promethean
