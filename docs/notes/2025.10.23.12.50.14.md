@codex
The `ContextStore` and `DualStoreManager` should continue to accept the same arguments as before.
We want these classes to be a thin wrappers around a new functional, idempotent, pure (as possible) interfaces.
We want these classes to emit depreciation warnings when used.
