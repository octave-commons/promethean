# shared/py/embedding_client.py

**Path**: `shared/py/embedding_client.py`

Chromadb embedding function that proxies requests to the embedding service.

## Dependencies
- chromadb
- requests

## Dependents
- services using the shared embedding service
