Fix build issues in @promethean/llm and @promethean/markdown-graph by adding a `js-yaml` type shim and switching broker service template import to `@promethean/legacy/serviceTemplate.js` with a shim. Verified both packages build.