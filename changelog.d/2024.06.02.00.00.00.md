- add a dry-run option to `github.pr.review.commentInline` so callers can validate resolved anchors without posting
- document that inline review comments currently target the HEAD (RIGHT) side of the diff only
